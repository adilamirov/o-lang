class JTest4 is

    method isEqual(Integer a, Integer b) : Boolean is
        result : Boolean(a.Equal(b))
        if result then
            return result
        end

        else_res : Boolean(0)
        return else_res
    end
end